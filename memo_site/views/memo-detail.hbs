
  <div class="row">
    
      <div class="col-lg-6">
        <div class="card">
          
          <div class="card-body">
            <span class="card-title" style="font-size: 36px; ">{{this.title}}</span>
            
            {{!-- <h6 class="card-subtitle mb-2 text-muted">Дата создания: {{this.createdAt}}</h6> --}}
            <div class="card-content"style="width: 400px;">
            <p class="card-text" style="white-space: pre-line; font-size: 24px;">{{this.description}}</p>
            </div>
            {{!-- <h6 class="card-text">Количество просмотров {{this.countView}}</h6> --}}
            
          </div>
        </div>
      </div>
    <button onclick= "document.location ='/memo-list/edit/{{this.id}}'">Редактировать</button>
  </div>
  <style>
    .card-content {
      width: 400px;
      margin-left: auto;
      margin-right: auto;
      text-align: left;
    }
  </style>

